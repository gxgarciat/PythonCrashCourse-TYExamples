# Use a variable to represent a person's name, and include some white space characters at the beginning
# and end of the name. Make sure you use each character combination "\t" and "\n", at least once.
# Print the name once, so the name is displayed. Then print the name using each of the three stripping
# functions, lstrip(), rstrip(), and strip()

personName = input("What is your name?: ")
print(personName)


